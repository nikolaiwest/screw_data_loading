CREATE TABLE IF NOT EXISTS screw_runs (
    id TEXT PRIMARY KEY,
    raw_json TEXT,
    format TEXT,
    node_id TEXT,
    nr INTEGER,
    result TEXT,
    hardware TEXT,
    mac0 TEXT,
    ip0 TEXT,
    sw_version TEXT,
    sw_build TEXT,
    MCE_transducer_function TEXT,
    MCE_measuring TEXT,
    MCE_factor REAL,
    max_speed INTEGER,
    location_name TEXT,
    channel TEXT,
    prg_nr INTEGER,
    prg_name TEXT,
    prg_date TEXT,
    cycle INTEGER,
    redundancy_sensor INTEGER,
    nominal_torque REAL,
    date TEXT,
    id_code TEXT,
    torque_unit TEXT,
    last_cmd TEXT,
    last_step_row TEXT,
    last_step_column TEXT,
    quality_code TEXT,
    total_time REAL,
    tool_serial INTEGER,
    redundancy_transducer_serial INTEGER,
    spindle_id TEXT,
    rework_code INTEGER,
    rework_text TEXT,
    batch_nr INTEGER,
    batch_canceled INTEGER,
    batch_direction_ok INTEGER,
    batch_direction_nok INTEGER,
    batch_max_ok INTEGER,
    batch_ok INTEGER,
    batch_max_nok INTEGER,
    batch_nok INTEGER,
    angle_global_threshold_nom REAL,
    angle_global_threshold_act REAL,
    torque_ct REAL,
    torque_cred REAL
);
